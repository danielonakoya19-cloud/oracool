# OraCool deployment dependencies: push delivery, encrypted admin credentials, and crypto QR codes.
pywebpush>=2.0
cryptography>=42
qrcode>=7.4        # crypto payment QR (pure-python SVG, no Pillow needed)
